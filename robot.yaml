tasks:
  rpachallenge:
    shell: python -m robot -d output -t "complete the challenge" tasks.robot

artifactsDir: output
condaConfigFile: shared/conda.yaml
ignoreFiles: [.gitignore]

PATH:
 - .
 - resources
PYTHONPATH:
 - .
 - libraries
 - resources
 - shared
 - shared/libraries
 - shared/resources
